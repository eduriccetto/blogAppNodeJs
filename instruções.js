/**   
 *               Aqui vamos prepara nosso projeto de BLOG o blogapp
 *      
 *      1- Primeiramente vamos instalar o pacote do express com o comando:
 *          
 *          $ npm install --save express
 * 
 *      2- Depois vamos instalar o express handlebars com o comando:
 * 
 *          $ npm install --save express-handlebars
 * 
 *      3- Depois vamos instalar o body-parser com o comando:
 * 
 *          $ npm install body-parser --save
 * 
 *      4- Depois vamos instalar o mongoose com o comando:
 * 
 *          $ npm install --save mongoose
 * 
 *   ##################################################################################
 * 
 *      - Nosso arquivo principal vai ser o app.js, que vai receber algumas configurações do express
 *  
 *      - Depois de criar a arquitetura de pastas com models, routes e views, vamos criar a pasra public
 *   onde estaremos instalando e configurando nosso bootstrap. Com o comando:
 * 
 *          $ npm install bootstrap
 * 
 *      - Depois das configurações de rotas e categorias, vamos preparar a validação do formuláro
 *   para isso precisaremos instalar duas ferramentas com os comandos:
 * 
 *          $ npm install --save express-session
 *          $ npm install --save connect-flash
 * 
 *      
 */